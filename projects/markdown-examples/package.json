{
  "private": true,
  "name": "markdown-examples",
  "scripts": {
    "build": "rimraf dist/* && tsc -b -f",
    "start": "yarn build && node -r tsconfig-paths/register dist/minimal.js"
  },
  "devDependencies": {
    "rimraf": "^3.0.2"
  }
}
